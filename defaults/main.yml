---
# directory path to files that must be correct
fix_directories: 
  - /home/zaher/Documents/cis-apache-backup/

# Files pattern to be correct 
files_pattern: '*.yml'

# Use regex in files_pattern or not
use_regex: false

# length max for each lines
line_max_length: 160
