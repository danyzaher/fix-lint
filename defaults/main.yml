---
# defaults file for fix-lint

# directory path to files that must be correct
fix_directories: 
  - /home/zaher/Documents/cis-apache-backup/
#  - /home/zaher/Bureau/cis-tomcat/
  
ansible_lint_rules: []

files_pattern: '*.yml'

use_regex: false

line_max_length: 160